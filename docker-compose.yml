services:
  workouts-to-plex:
    build: .
    restart: unless-stopped
    volumes:
      - .:/config                                 # workout definitions (reads /config/workouts.yaml)
      - ./images:/input                          # generated PNGs land here (intermediate)
      - ./plex:/output                           # point Plex library at this directory
